{
    "definitions": {
        "mapping": {
            "description": "Defines a mapping between two fields A and B, where B is equivalent to A with  some subset of its fields prefilled as specified in the mapping. The prefilled  fields must not be objects - but can take strings or numbers or lists as values.\n", 
            "properties": {
                "field": {
                    "description": "name of field to map", 
                    "type": "string"
                }, 
                "maps_to": "name of type mapped to", 
                "with": {
                    "description": "A dict properties of the mapped to field type (keys) and fillers (values).\n", 
                    "type": "object"
                }
            }, 
            "type": "object"
        }
    }, 
    "properties": {
        "mappings": {
            "items": {
                "$ref": "#/definitions/mapping"
            }
        }
    }
}